{
  "name": "github-organization-statistics",
  "version": "1.0.0",
  "description": "Gathers statistics across a GitHub Organization",
  "main": "index.js",
  "repository": "git@github.com:dallasclark/github-organization-statistics.git",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "yarn clean:dependencies && yarn clean:dist",
    "clean:dependencies": "rm -rf ./node_modules",
    "clean:dist": "rm -rf ./dist",
    "develop": "tsx watch ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2"
  }
}
